[project]
name = "codewidget"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.10.2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths  = ["tests"]

[dependency-groups]
dev = [
    "pillow>=12.1.1",
    "pyinstaller>=6.19.0",
    "pytest>=9.0.2",
    "pytest-qt>=4.5.0",
]
