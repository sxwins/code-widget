# 12 测试报告（Test Report）

| 项目 | 内容 |
|------|------|
| 文档版本 | 1.0 |
| 执行日期 | 2026-03-03 |
| 适用版本 | CodeWidget v0.1.0 |
| 执行人 | 开发者兼测试 |
| 执行平台 | Windows 11 Home 10.0.26100 |

---

## 1. 执行摘要

| 指标 | 结果 |
|------|------|
| 测试用例总数 | 38 |
| **通过（PASS）** | **38** |
| 失败（FAIL） | 0 |
| 跳过（SKIP） | 0 |
| 总执行时间 | ~2.33 秒 |
| 执行命令 | `uv run pytest -v` |

**结论：所有测试通过，核心业务逻辑和关键 GUI 控件行为符合规格。**

---

## 2. 测试结果详情

### 2.1 按文件汇总

| 测试文件 | 件数 | 通过 | 失败 | 通过率 |
|---------|------|------|------|-------|
| `test_scheduler.py` | 17 | 17 | 0 | 100% |
| `test_override.py` | 10 | 10 | 0 | 100% |
| `test_attendance_window.py` | 3 | 3 | 0 | 100% |
| `test_config_dialog.py` | 5 | 5 | 0 | 100% |
| `test_tray.py` | 3 | 3 | 0 | 100% |
| **合计** | **38** | **38** | **0** | **100%** |

### 2.2 按功能域汇总

| 功能域 | 件数 | 通过 | 通过率 |
|-------|------|------|-------|
| 排课引擎（件数 + 日期） | 12 | 12 | 100% |
| 显示窗口计算 | 5 | 5 | 100% |
| 调课规则（skip / makeup / reschedule） | 10 | 10 | 100% |
| GUI 控件（状态验证） | 11 | 11 | 100% |

### 2.3 按优先级汇总

| 优先级 | 件数 | 通过 | 失败 |
|-------|------|------|------|
| P1（核心功能） | 25 | 25 | 0 |
| P2（重要功能） | 13 | 13 | 0 |
| P3 | 0 | — | — |

---

## 3. 缺陷汇总

### 3.1 本次测试执行发现的缺陷

**在自动化测试覆盖范围内，未发现任何缺陷。**

### 3.2 测试过程中发现的文档问题（已修正）

在测试过程中发现了文档本身的若干不准确之处，均已在文档审查阶段修正，不构成代码缺陷：

| 文档 | 问题描述 | 状态 |
|------|---------|------|
| `04_数据格式规格.md` | Q2/Q4 slots 数量错误（1→2） | ✅ 已修正（[1.0.8]） |
| `05_界面规格.md` | 外观默认字体误写 "Courier New"（应为 "Arial"） | ✅ 已修正（[1.0.9]） |
| `06_数据流图.md` | session_key 示例未零填充；透明度误列为可配置属性 | ✅ 已修正（[1.0.10]） |

---

## 4. 测试覆盖分析

### 4.1 已覆盖区域

| 区域 | 覆盖情况 |
|------|---------|
| 排课算法核心路径 | ✅ 完整覆盖（所有课程类型、边界日期、session_key 编号） |
| 调课规则三类型 | ✅ 完整覆盖（skip / makeup / reschedule + 多 slot 场景） |
| 显示窗口计算 | ✅ 完整覆盖（含边界值：窗口外−1分 / 边界恰好 / 窗口内 / 非授课日） |
| GUI 控件状态 | ✅ 主要状态覆盖（标签更新、初始可见性、对话框结构） |

### 4.2 未覆盖区域（测试盲区）

| 未覆盖区域 | 影响等级 | 说明 |
|-----------|---------|------|
| `main.py` 集成流程（`_tick`、信号连接、闭包状态） | **高** | 应用最终行为的集成验证完全依赖手动测试 |
| 错误处理路径（JSON 解析失败、文件不存在） | 中 | 异常分支无自动化覆盖 |
| 配置文件读写（`save_teacher_config`、`save_app_settings`） | 中 | 持久化逻辑无自动化覆盖 |
| `_code_for()` 函数（临时码优先级逻辑） | 中 | 定义于闭包内，无法直接访问 |
| `on_temp_code_entered/cleared` 处理函数 | 中 | 同上 |
| 用户交互流程（设置保存、标签页切换） | 低 | 当前仅做静态结构验证 |
| macOS 平台行为（`Tool` 标志、`.app` 路径） | 低 | 未在 macOS 执行测试 |
| 视觉外观（字体渲染、布局、透明度效果） | 低 | 非功能性，手动验收即可 |

---

## 5. 遗留风险评估

| 风险项 | 等级 | 缓解措施 |
|-------|------|---------|
| 主集成流程（`main.py`）无自动化保障 | ⚠️ 中高 | 每次发布前执行完整手动冒烟测试（启动→显示→隐藏→保存配置→退出） |
| 无 CI/CD，提交后不自动触发测试 | ⚠️ 中 | 本地开发时手动运行 `uv run pytest -v` 作为提交前检查 |
| 测试依赖真实配置文件，文件变更会导致失败 | ⚠️ 低中 | 将 `config/teacher_A_config.json` 锁定为测试专用版本，不随业务数据更新 |
| macOS 未经自动化测试 | ⚠️ 低 | macOS 发布前在目标机器执行手动验收 |

---

## 6. 发布建议

基于本次测试结果：

| 条件 | 状态 |
|------|------|
| 所有自动化测试通过 | ✅ |
| 无 P1 级缺陷 | ✅ |
| 核心路径（排课 + 调课 + 显示窗口）覆盖率 100% | ✅ |
| 主要 GUI 控件结构验证通过 | ✅ |
| 已知测试盲区已在文档中明确说明 | ✅ |

**建议：可以发布。**

在满足以下补充条件后执行发布：
1. 在目标平台（Windows / macOS）完成手动冒烟测试（启动 → 正常课程时段内显示出勤码 → 课程时段外自动隐藏 → 设置保存后立即生效）
2. 确认 `config/teacher_A_config.json` 中的当年度课程数据与 `school_config.json` 的学期日历一致
