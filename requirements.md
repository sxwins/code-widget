# 出勤码展示工具 需求说明文档（中文）

## 1. 文档目的
本需求说明文档用于明确“出勤码展示工具”的业务范围、数据逻辑、运行规则与界面要求，作为后续原型设计与开发实现的依据。

---

## 2. 背景与问题
学校现行出勤系统要求教师在课程开始前后的一段时间内向学生提示“出勤码”，学生在既有系统中输入该码完成出勤登记。

现状中各教师以 PowerPoint、临时文本、手工展示等方式呈现出勤码，存在：
- 展示方式分散、重复操作多
- 上课时易忘记展示或展示时间不准确
- 缺少统一的“按课表自动提示”机制
- 投屏时不便：窗口不置顶、容易被遮挡、码不够醒目

因此需要一个**仅用于展示出勤码**的轻量工具，提高课堂出勤码展示效率与准确性。

---

## 3. 系统范围（Scope）

### 3.1 本工具包含的功能（In Scope）
- 基于全校通用的学期课程时间表与每日固定时限表，推导“某门课的每一次上课日期（第1回、第2回…）”
- 教师以“周几第几节对应课程名”的简单方式录入个人课程信息，并生成个人配置文件
- 上课前/上课后指定窗口期内，工具能够定位当前应显示的课程，并在小窗口中展示出勤码
- 提供一个**小尺寸、可拖动、始终置顶**的出勤码展示窗口，便于投屏或边授课边展示
- 支持教师手动输入/粘贴出勤码并展示（码来源由教师自行从既有系统获得）
- 支持教师**查看并修改**自己的授课日/课时配置（用于临时停课、代课、补课等情况，详见第 6.6 节）

### 3.2 本工具不包含的功能（Out of Scope）
- 不负责学生身份验证、出勤登记、提交记录、统计报表等（由现有系统完成）
- 不与学校既有系统进行接口对接（不自动获取出勤码、不自动提交出勤数据）
- 暂不支持集中讲义/集中授课等特殊课表形态（本阶段仅处理“普通课”）

---

## 4. 角色与使用场景

### 4.1 角色
- 教师（唯一主要用户）

### 4.2 核心使用场景
1. 学期开始前：教师导入/配置全校学期时间表与时限表（通常可复用模板）  
2. 教师设置个人课程：例如“周一2限：课程A；周三3限：课程B”，生成个人配置文件  
3. 上课当天：教师启动工具  
4. 在“开课前10分钟～开课后30分钟”的窗口期内，工具显示对应课程的出勤码展示窗口  
5. 教师将出勤码粘贴/输入到窗口中，窗口始终置顶、可拖动，方便投屏给学生观看  
6. 超过窗口期后，窗口自动隐藏或变为待机状态（行为可配置）  
7. （新增）如遇出差/生病/停课/补课等情况，教师可在工具中查看并修改某门课在特定日期的授课安排，使显示逻辑与实际授课一致

---

## 5. 数据逻辑与配置模型
本工具的数据分为三层：**全校通用配置** + **个人课程配置** + **运行时状态**。

### 5.1 全校通用配置（School Config）

#### 5.1.1 学期课表（日历/授课日安排）
面向“普通课”，全校存在一个学期层面的统一安排，用于确定：
- 学期名称（如“2026春季学期”）
- 学期起止日期
- 每个“星期几”的普通课程上课日序列（例如：周一课第1回~第14回分别是哪些日期；周二同理……）
  - 注：由于祝日、学校活动、休讲日等影响，某些周次可能不上课，因此**不能仅用“每周”推导**，必须能表达“实际授课日”。

> 需求要点：系统必须能够从该配置中得到“周X上课的每一次日期列表”，以支持第1回、第2回的准确推导。

#### 5.1.2 每日时限表（Periods）
- 一天固定 7 个课时段（1限～7限）
- 各课时段的开始时间与结束时间固定、全校一致

### 5.2 个人课程配置（Teacher Config）
教师个人以简洁方式输入：
- 课程列表（课程名/简称）
- 每门课程对应的上课槽位：`星期几 + 第几限`
  - 例如：课程A＝周一2限；课程B＝周三3限

该信息需要能够保存为一个文件（个人配置文件），供工具运行时读取。

### 5.3 例外/调整配置（Overrides / Exceptions）（新增）
为支持教师因出差/生病/停课等导致的“实际授课与原课表不一致”，需要在个人侧引入“例外规则”：

- **停课（Skip）**：某门课在某个授课日不授课  
- **补课（Makeup）**：新增一次授课日（可能改变日期/时限）  
- **调课（Reschedule）**：将某次授课从原日期/时限调整到另一个日期/时限

> 这些调整仅影响“本工具的展示逻辑”，不改变全校通用课表文件本身。

### 5.4 运行时状态（Runtime State）
- 当前日期时间
- 当前命中的课程（如果存在）
- 当前显示的出勤码文本（由教师手动输入/粘贴）
- 窗口位置（拖动后可记忆）
- （可选）最近一次显示/隐藏状态

---

## 6. 业务规则（核心逻辑）

### 6.1 授课日期推导规则（基础）
输入：
- 学期课表（按星期几提供的授课日期序列，或等价表示）
- 教师课程配置（课程 → 星期几/第几限）

输出：
- 每门课的“授课日期列表”（第1回、第2回…对应日期）
- 若需要，可进一步得到“某日期属于第几回”

说明：
- 若学期课表以“周一第1回～第14回日期列表”形式提供，则每个课程只需引用对应星期几的日期序列即可。
- 本阶段仅考虑普通课；集中讲义等不纳入推导。

### 6.2 出勤码显示窗口规则
对某门课在某个授课日期：
- 课程开始时间 = 当天该“限”的开始时间
- **显示窗口**：
  - 开始：课程开始前 10 分钟
  - 结束：课程开始后 30 分钟
- 当系统时间落在显示窗口内时，工具应显示该课程的出勤码窗口。

### 6.3 多课程冲突处理
若同一时刻命中多门课（理论上少见，但配置可能导致）：
- 优先显示“窗口开始时间更接近当前时间”的课程；或按更小的课时序号优先
- 需定义确定性规则，避免显示来回跳动

### 6.4 非窗口期行为
当系统时间不在任何课程窗口期内：
- 工具默认进入待机/隐藏状态（可配置）
- 允许教师手动打开窗口（可选功能，建议保留以应对临时情况）

### 6.5 出勤码录入规则
- 出勤码由教师手动输入/粘贴到窗口
- 工具仅负责展示，不校验、不上报

### 6.6 课时/授课日调整规则（新增）
教师可对“个人课程的具体某一次授课”进行调整，调整优先级高于基础课表推导结果。

#### 6.6.1 停课（Skip）
- 若教师将某门课在某个授课日标记为“停课”，则该日不再触发出勤码展示窗口。
- 若停课发生在当天且已进入显示窗口期，工具应立即隐藏/退出该课程展示（或切换到待机）。

#### 6.6.2 补课（Makeup）
- 教师可为某门课添加一个新的授课日期，并指定对应时限（1～7限）。
- 在补课日期的显示窗口期内，应触发该门课的出勤码展示。

#### 6.6.3 调课（Reschedule）
- 教师可将某一次授课从“原日期/原时限”调整到“新日期/新时限”。
- 调课可以视为“原日期停课 + 新日期补课”的组合，但在界面上应作为一个动作呈现，便于理解与回溯。

#### 6.6.4 调整对“第几回”的影响（建议规则）
为减少复杂度并保持可解释性，建议采用：
- **回次以“实际授课发生顺序”编号**（在应用了停课/补课/调课之后重新排序）。
- 工具应提供回次预览，帮助教师确认调整后的第1回～第14回对应日期是否正确。

---

## 7. 功能需求（Functional Requirements）

### 7.1 配置管理
- 支持加载“全校通用配置文件”（学期课表 + 7限时间表）
- 支持加载/保存“个人配置文件”（教师课程列表与槽位）
- 支持通过简单输入生成个人配置（例：添加课程名、选择周几与第几限）
- （新增）支持查看并编辑个人配置中的“授课调整”（停课/补课/调课）

### 7.2 运行与提示
- 启动后可自动判断“当前是否处于某课的显示窗口期”
- 命中时展示窗口，未命中时隐藏/待机
- 窗口显示的课程信息应明确（课程名、星期几、第几限、日期、距离开课倒计时）
- （新增）展示逻辑应综合基础课表与教师的调整配置，且调整优先

### 7.3 出勤码展示窗口
- 允许教师输入/粘贴出勤码
- 码需醒目（大字体/高对比度，具体视觉后续设计）
- 提供一键清空/替换码（可选）

### 7.4 调整与预览（新增）
- 教师可查看每门课的授课日历预览（例如第1回～第14回日期）
- 教师可对某日期执行：停课 / 调课 / 补课
- 调整后预览应即时更新，并可保存到个人配置文件
- 应支持撤销/删除某条调整记录（至少支持删除，撤销可选）

---

## 8. 界面与交互需求（UI/UX Requirements）

### 8.1 小窗口（核心）
- 小尺寸、占屏幕一小块区域
- **始终置顶（Always-on-top）**
- 可用鼠标**拖动移动位置**
- 支持记忆窗口位置（下次启动恢复）
- 不干扰授课操作（不抢焦点/尽量简洁）

### 8.2 配置界面（非授课时使用）
- 提供简洁表单维护课程列表与槽位
- 提供导入/选择学期通用配置
- 提供授课日预览：某门课第1回～第14回日期（建议必做，用于验证课表正确性）
- （新增）提供“授课调整”编辑区：
  - 停课：选择日期 → 标记停课
  - 补课：选择日期 + 选择时限 → 添加
  - 调课：选择原日期 → 选择新日期 + 新时限 → 保存
  - 支持删除某条调整记录
- 需要明确提示：调整仅影响本工具展示，不影响学校系统/官方课表

---

## 9. 技术要求（Technical Requirements）

### 9.1 编程语言
- 使用 **Python** 或 **C++** 实现
- 优先推荐 Python（开发效率高，GUI 生态成熟，适合此类轻量工具）
- 若选用 Python，GUI 框架首选 **PyQt6** 或 **PySide6**（Qt 原生支持置顶窗口、可拖动、系统托盘）
- 若选用 C++，同样采用 **Qt6** 框架

### 9.2 跨平台支持
- 必须同时支持 **Windows**（10/11）和 **macOS**
- 不依赖平台特有 API；所有 GUI、文件操作均使用跨平台接口
- 发布形态：
  - Windows：独立 `.exe`（通过 PyInstaller 或 Qt 部署工具打包）
  - macOS：独立 `.app` 应用包

### 9.3 依赖与分发
- 运行时无需用户手动安装 Python 或任何依赖（打包为独立可执行文件）
- 配置文件使用纯文本格式（JSON），可手动编辑、跨平台可移植

---

## 10. 非功能性需求（Non-Functional Requirements）
- 启动迅速、操作步骤少（上课时通常只有几十秒空档）
- 本地运行优先（不依赖网络）
- 配置文件可移植（换电脑可带走）
- 稳定性：不因系统睡眠/时间变化导致崩溃；时间判断需鲁棒
- 可维护性：配置结构清晰、可版本化
- 可解释性：能让教师看懂”为什么今天会/不会显示”，并能通过预览验证

---

## 11. 假设与约束
- 学期课表（按星期几提供的授课日期序列）可由用户以文件方式提供或维护
- 每日 7 限时间固定且全校一致
- 用户（教师）能够从现有系统获取出勤码并手动输入/粘贴
- 暂不支持集中讲义、复杂调课规则引擎、跨校区差异等（后续迭代再讨论）
- 个人侧调整功能用于应对个别教师的停课/补课情况，不要求与学校官方排课同步

---

## 12. 验收标准（Acceptance Criteria）
1. 教师能用“周几第几限→课程名”方式配置个人课程，并保存为文件  
2. 工具能基于学期课表正确识别某一天是否有该课程，并能标识第几回（如提供预览）  
3. 在开课前10分钟到开课后30分钟内，工具能自动显示对应课程的置顶小窗  
4. 小窗可拖动、始终置顶、占用屏幕面积小  
5. 教师可在小窗中粘贴出勤码，并以醒目方式展示给学生  
6. 超过窗口期后，小窗按预期自动隐藏/待机（或按配置行为执行）  
7. （新增）教师能查看课程授课日预览，并对特定日期进行停课/补课/调课修改，修改后展示逻辑按调整生效且可保存  

---

## 13. 后续可扩展（Roadmap）
- 支持集中讲义/特殊排课
- 支持更复杂的调课规则（例如全校统一的节假日调补）
- 多学期、多套课表快速切换
- 快捷键（快速显示/隐藏、清空码）
- 多显示器投屏优化（记忆显示器与位置）
- 配置导入导出向导、模板库